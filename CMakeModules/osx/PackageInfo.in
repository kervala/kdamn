<?xml version="1.0" encoding="utf-8"?>
<pkg-info overwrite-permissions="true" relocatable="false" identifier="${MACOSX_BUNDLE_GUI_IDENTIFIER}" postinstall-action="none" version="1.2.565" format-version="2" generator-version="InstallCmds-662.1 (17B1003)" install-location="/Applications" auth="root" preserve-xattr="true" minimumSystemVersion="${CMAKE_OSX_DEPLOYMENT_TARGET}">
    <payload numberOfFiles="${NUMBER_OF_FILES}" installKBytes="${INSTALL_KBYTES}"/>
    <bundle path="./${PRODUCT_FIXED}.app" id="${MACOSX_BUNDLE_GUI_IDENTIFIER}" CFBundleShortVersionString="${VERSION}" CFBundleVersion="${VERSION}"/>
    <bundle-version>
        <bundle id="${MACOSX_BUNDLE_GUI_IDENTIFIER}"/>
    </bundle-version>
    <upgrade-bundle>
        <bundle id="${MACOSX_BUNDLE_GUI_IDENTIFIER}"/>
    </upgrade-bundle>
    <update-bundle/>
    <atomic-update-bundle/>
    <strict-identifier>
        <bundle id="${MACOSX_BUNDLE_GUI_IDENTIFIER}"/>
    </strict-identifier>
    <relocate>
        <bundle id="${MACOSX_BUNDLE_GUI_IDENTIFIER}"/>
    </relocate>
</pkg-info>
